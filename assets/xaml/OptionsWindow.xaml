<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="$(T 'OptionsTitle' 'Opciones — SysOpt')" Width="460" Height="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="$(Get-TC 'BgDeep' '#0D0F1A')" WindowStyle="SingleBorderWindow">
    <Grid>
        <Rectangle Fill="$(Get-TC 'BgDeep' '#0D0F1A')"/>
        <Ellipse Width="300" Height="300" Opacity="0.08" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-80,-60,0,0">
            <Ellipse.Fill><RadialGradientBrush><GradientStop Color="$(Get-TC 'AccentPurple' '#A47CFF')" Offset="0"/><GradientStop Color="Transparent" Offset="1"/></RadialGradientBrush></Ellipse.Fill>
        </Ellipse>

        <DockPanel Margin="28,24,28,24" LastChildFill="True">
            <!-- Botones anclados al fondo -->
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Name="btnOptApply" Content="$(T 'OptionsBtnApply' 'Aplicar')"
                        Width="110" Height="34" Margin="0,0,10,0"
                        Background="$(Get-TC 'BtnSecondaryBg' '#1A4A8A')" BorderBrush="$(Get-TC 'AccentBlue' '#5BA3FF')" BorderThickness="1"
                        Foreground="$(Get-TC 'AccentBlue' '#5BA3FF')" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" CornerRadius="8" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="$(Get-TC 'HdrBtnHover' '#253060')"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Name="btnOptClose" Content="$(T 'OptionsBtnClose' 'Cerrar')"
                        Width="110" Height="34"
                        Background="$(Get-TC 'HdrBtnBg' '#1A2040')" BorderBrush="$(Get-TC 'HdrBtnBorder' '#3D5080')" BorderThickness="1"
                        Foreground="$(Get-TC 'TextSecondary' '#9BA4C0')" FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bd" CornerRadius="8" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bd" Property="Background" Value="$(Get-TC 'HdrBtnHover' '#253060')"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>

            <!-- Contenido principal -->
            <StackPanel>
                <!-- Header -->
                <TextBlock FontFamily="Segoe UI" FontSize="20" FontWeight="Bold" Foreground="$(Get-TC 'TextPrimary' '#E8ECF4')" Margin="0,0,0,18">
                    <Run Text="⚙ "/><Run Foreground="$(Get-TC 'AccentPurple' '#A47CFF')" Text="$(T 'OptionsTitle' 'Opciones')"/>
                </TextBlock>

                <!-- Tema -->
                <TextBlock FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Foreground="$(Get-TC 'TextSecondary' '#9BA4C0')" Margin="0,0,0,6"
                           Text="$(T 'OptionsThemeLabel' 'Tema visual')"/>
                <ComboBox Name="cmbTheme" Height="32" Margin="0,0,0,14"/>

                <!-- Idioma -->
                <TextBlock FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold" Foreground="$(Get-TC 'TextSecondary' '#9BA4C0')" Margin="0,0,0,6"
                           Text="$(T 'OptionsLangLabel' 'Idioma')"/>
                <ComboBox Name="cmbLang" Height="32" Margin="0,0,0,18"/>

                <!-- Debug Logging — Win11 Toggle Switch -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,14" UseLayoutRounding="True">
                    <ToggleButton Name="tglDebugLog" Width="50" Height="26" Cursor="Hand"
                                  OverridesDefaultStyle="True" Focusable="False">
                        <ToggleButton.Template>
                            <ControlTemplate TargetType="ToggleButton">
                                <Grid Width="50" Height="26">
                                    <!-- Track — OFF: outlined pill / ON: solid accent -->
                                    <Border x:Name="trackBorder" CornerRadius="13"
                                            Background="Transparent"
                                            BorderBrush="$(Get-TC 'TextMuted' '#666666')"
                                            BorderThickness="2"/>
                                    <!-- Thumb OFF — small circle, left aligned -->
                                    <Border x:Name="thumbOff" Width="16" Height="16" CornerRadius="8"
                                            Background="$(Get-TC 'TextMuted' '#666666')"
                                            HorizontalAlignment="Left" VerticalAlignment="Center"
                                            Margin="5,0,0,0"/>
                                    <!-- Thumb ON — circle, right aligned (hidden default) -->
                                    <Border x:Name="thumbOn" Width="18" Height="18" CornerRadius="9"
                                            Background="$(Get-TC 'BtnPrimaryFg' '#FFFFFF')"
                                            HorizontalAlignment="Right" VerticalAlignment="Center"
                                            Margin="0,0,4,0" Visibility="Collapsed"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <!-- Checked / ON -->
                                    <Trigger Property="IsChecked" Value="True">
                                        <Setter TargetName="trackBorder" Property="Background"
                                                Value="$(Get-TC 'BtnPrimaryBg' '#4AE896')"/>
                                        <Setter TargetName="trackBorder" Property="BorderBrush"
                                                Value="$(Get-TC 'BtnPrimaryBg' '#4AE896')"/>
                                        <Setter TargetName="thumbOff" Property="Visibility" Value="Collapsed"/>
                                        <Setter TargetName="thumbOn"  Property="Visibility" Value="Visible"/>
                                    </Trigger>
                                    <!-- Hover -->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="trackBorder" Property="Opacity" Value="0.80"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </ToggleButton.Template>
                    </ToggleButton>
                    <TextBlock FontFamily="Segoe UI" FontSize="12"
                               Foreground="$(Get-TC 'TextSecondary' '#9BA4C0')"
                               VerticalAlignment="Center" Margin="12,0,0,0"
                               Text="$(T 'OptionsDebugLog' '🔍 Habilitar logs de depuración (DBG)')"/>
                </StackPanel>

                <!-- Hint -->
                <TextBlock FontFamily="Segoe UI" FontSize="10" FontStyle="Italic" Foreground="$(Get-TC 'TextMuted' '#5A6080')"
                           Text="$(T 'OptionsRestartHint' 'Algunos cambios de idioma requieren reiniciar SysOpt.')"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>